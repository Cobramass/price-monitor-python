"""Price & stock monitor — Python edition.

A production-shaped scraping/data-feed package: async fetching with retry and
bounded concurrency, a clean normalised schema, and change-detection between
runs. Mirrors the Node `price-monitor` demo so the two read as a matched pair.
"""

__version__ = "1.0.0"
